# Backend Environment Variables
# Copy this file to .env and fill in your values

# ===== TESTING CONFIGURATION =====
# Use these values when testing locally

# Environment
NODE_ENV=development

# Server Configuration
PORT=3000

# Supabase Configuration
# IMPORTANT: Use SERVICE_ROLE_KEY (not anon key) for backend
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Solana Configuration
# Public RPC endpoint (free, rate-limited)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Token Contract Address (Solana SPL Token Mint Address)
# For testing: Use any existing Solana SPL token mint address
# Example: USDC on mainnet: EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v
TOKEN_CONTRACT_ADDRESS=YOUR_TEST_TOKEN_MINT_ADDRESS_HERE

# Admin Wallet Addresses (comma-separated)
# Add your admin wallet address(es) here
ADMIN_ADDRESSES=YOUR_ADMIN_WALLET_ADDRESS_HERE

# CORS Configuration
# Allow requests from your frontend (local development)
CORS_ORIGIN=http://localhost:5173

# JWT Secret (for admin authentication)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-random-jwt-secret-here

# Reward Wallet Private Key (for sending SOL rewards)
# IMPORTANT: Keep this secret! Never commit to git!
# This is the wallet that will send SOL to winners
# Generate a new wallet for this purpose and fund it with SOL
# Format: Base58-encoded private key from Phantom/Solflare export
REWARD_WALLET_PRIVATE_KEY=your-base58-encoded-private-key-here


# ===== PRODUCTION CONFIGURATION =====
# Use these values when deploying to Render

# Environment
# NODE_ENV=production

# Server Configuration
# PORT=3000

# Supabase Configuration (production)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_KEY=your-service-role-key-here

# Solana Configuration (production)
# For better performance, use a paid RPC provider:
# - Helius: https://helius.dev
# - QuickNode: https://quicknode.com
# - Alchemy: https://alchemy.com
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Token Contract Address (YOUR OWN TOKEN)
# TOKEN_CONTRACT_ADDRESS=YOUR_ACTUAL_TOKEN_MINT_ADDRESS_HERE

# Admin Wallet Addresses (comma-separated, production addresses)
# ADMIN_ADDRESSES=wallet1,wallet2,wallet3

# CORS Configuration (production)
# CORS_ORIGIN=https://your-app.vercel.app

# JWT Secret (production - use a strong random value)
# JWT_SECRET=your-production-jwt-secret-here

# Reward Wallet Private Key (production)
# REWARD_WALLET_PRIVATE_KEY=your-base58-encoded-private-key-here


# ===== WHAT TO CHANGE FOR LAUNCH =====
# 1. Replace TOKEN_CONTRACT_ADDRESS with YOUR token's mint address
# 2. Set ADMIN_ADDRESSES to your admin wallet address
# 3. Generate a new JWT_SECRET using the command above
# 4. Update CORS_ORIGIN to your Vercel frontend URL
# 5. Set up a reward wallet and add its private key (REWARD_WALLET_PRIVATE_KEY)
# 6. Update Supabase credentials to production project
# 7. Consider using a paid RPC provider for better performance


# ===== IMPORTANT NOTES =====
# - NEVER use the anon key for backend - always use SERVICE_ROLE_KEY
# - The reward wallet private key should be a NEW wallet, not your main wallet
# - Fund the reward wallet with enough SOL for payouts
# - Keep all production secrets safe and never commit .env to git

