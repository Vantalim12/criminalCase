{
  "buildCommand": "npm install && npm run build --workspace=packages/types && npm run build --workspace=packages/config && npm run build --workspace=frontend",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
